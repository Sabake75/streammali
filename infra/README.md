# Infra

Orchestration locale des services StreamMali (une fois les apps scaffoldées) : API Laravel, PostgreSQL, Redis, Web Next.js.

Un `docker-compose.yml` sera ajouté ici pour le dev local une fois `apps/api` et `apps/web` initialisés.
